# downloadkit TODO

## Goal

Build `downloadkit` as the lean file transfer layer for personal use:

- robust single-file downloads
- resumable downloads
- atomic writes
- fallback URLs
- response/content validation

This should be the layer that handles "take a URL and make the file land correctly on disk."

## Package shape

- [x] Create `downloadkit/pyproject.toml`
- [x] Add `downloadkit` to the uv workspace members in the root [pyproject.toml](/home/jake/Developer/dl/pyproject.toml)
- [x] Start with these modules: `download.py`, `resume.py`, `validate.py`, `paths.py`, `diagnostics.py`, `cli.py`

## V1 library tasks

- [x] Define a simple `DownloadConfig` for destination path, temp-file strategy, overwrite/skip policy, resume policy, timeout, retries, and validation options
- [x] Implement atomic download-to-temp then replace
- [x] Implement HTTP `Range` resume
- [x] Implement fallback URL support
- [x] Implement content-type and file-signature validation hooks
- [x] Implement "HTML received where binary was expected" detection

## V1 CLI tasks

- [x] Define the first CLI surface: `downloadkit fetch URL`
- [x] Add a root-level wrapper command in the parent `dl` folder
- [x] Support `-o/--output`, `--cookies`, `--resume`, `--overwrite`, `--retry`, `--timeout`, `--rate`, `--header`, `--fallback`, and `--json`
- [x] Return simple machine-readable result output for success, skip, resume, and failure cases

## Integration

- [x] Accept a preconfigured `requests.Session` or `requestkit` session object
- [x] Accept `cookies.txt` generated by `cookiekit`
- [x] Keep retry/timeout behavior consistent with `requestkit` where possible

## Quality

- [x] Add tests for resume and partial-content handling
- [x] Add tests for invalid HTML/challenge-page responses during binary downloads
- [x] Add tests for fallback URL behavior
- [x] Add tests for atomic replace and interrupted-download cleanup
