# downloadkit

`downloadkit` is the planned file-transfer layer for this workspace.

Intended scope:

- atomic file writes
- resume support
- fallback URLs
- response validation
- robust single-file fetches

Status:

- package scaffold exists
- implementation has not started yet
